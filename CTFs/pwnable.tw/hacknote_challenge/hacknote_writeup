
┌─────────────┬───────────────────────────[ hacknote Writeup ]──────────────────────────────────────────
| Description |
└─────────────┘
Decompile

main() {
  int iVar1;                                                 // choice input as integer
  int in_GS_OFFSET;
  char local_18 [4];                                         // choice input for read - char array
  undefined4 local_14;                                       // canary
  undefined1 *puStack_c;                                     // ???

  puStack_c = &stack0x00000004;
  local_14 = *(undefined4 *)(in_GS_OFFSET + 0x14);
  setvbuf(stdout,(char *)0x0,2,0);
  setvbuf(stdin,(char *)0x0,2,0);
  do {
    while( true ) {
      while( true ) {
        print_instr();                                       // Prints instructions to console
        read(0,local_18,4);                                  // read input into local_18 
        iVar1 = atoi(local_18);                              // set input into iVar1
        if (iVar1 != 2) break;
        delete_note();                                       // If 'delete note' then call delete_note() 
      }
      if (2 < iVar1) break;
      if (iVar1 == 1) {
        add_note();                                          // If 'add note' then call add_note()
      }
      else {
LAB_08048a96:
        puts("Invalid choice");
      }
    }
    if (iVar1 != 3) {
      if (iVar1 == 4) {
        exit(0);                                             // If 'exit' then exit
      }
      goto LAB_08048a96;
    }
    print_note();                                            // If 'print note' then call print_note()
  } while( true );
}


delete_note() {
  int iVar1;
  int in_GS_OFFSET;
  char local_14 [4];
  int local_10;

  local_10 = *(int *)(in_GS_OFFSET + 0x14);
  printf("Index :");
  read(0,local_14,4);
  iVar1 = atoi(local_14);
  if ((iVar1 < 0) || (DAT_0804a04c <= iVar1)) {
    puts("Out of bound!");
    _exit(0);
  }
  if (*(int *)(&DAT_0804a050 + iVar1 * 4) != 0) {
    free(*(void **)(*(int *)(&DAT_0804a050 + iVar1 * 4) + 4));
    free(*(void **)(&DAT_0804a050 + iVar1 * 4));
    puts("Success");
  }
  if (local_10 != *(int *)(in_GS_OFFSET + 0x14)) {
    __stack_chk_fail();
  }
  return;
}


add_note() {
  int iVar1;
  void *pvVar2;
  size_t __size;
  int in_GS_OFFSET;
  int local_20;
  char local_18 [8];
  int local_10;

  local_10 = *(int *)(in_GS_OFFSET + 0x14);
  if (DAT_0804a04c < 6) {
    for (local_20 = 0; local_20 < 5; local_20 = local_20 + 1) {
      if (*(int *)(&DAT_0804a050 + local_20 * 4) == 0) {
        pvVar2 = malloc(8);
        *(void **)(&DAT_0804a050 + local_20 * 4) = pvVar2;
        if (*(int *)(&DAT_0804a050 + local_20 * 4) == 0) {
          puts("Alloca Error");
          exit(-1);
        }
        **(undefined4 **)(&DAT_0804a050 + local_20 * 4) = FUN_0804862b;
        printf("Note size :");
        read(0,local_18,8);
        __size = atoi(local_18);
        iVar1 = *(int *)(&DAT_0804a050 + local_20 * 4);
        pvVar2 = malloc(__size);
        *(void **)(iVar1 + 4) = pvVar2;
        if (*(int *)(*(int *)(&DAT_0804a050 + local_20 * 4) + 4) == 0) {
          puts("Alloca Error");
          exit(-1);
        }
        printf("Content :");
        read(0,*(void **)(*(int *)(&DAT_0804a050 + local_20 * 4) + 4),__size);
        puts("Success !");
        DAT_0804a04c = DAT_0804a04c + 1;
        break;
      }
    }
  }
  else {
    puts("Full");
  }
  if (local_10 != *(int *)(in_GS_OFFSET + 0x14)) {
    __stack_chk_fail();
  }
  return;
}


print_note() {
  int iVar1;
  int in_GS_OFFSET;
  char local_14 [4];
  int local_10;
   
  local_10 = *(int *)(in_GS_OFFSET + 0x14);
  printf("Index :");
  read(0,local_14,4);
  iVar1 = atoi(local_14);
  if ((iVar1 < 0) || (DAT_0804a04c <= iVar1)) {
    puts("Out of bound!");
    _exit(0);
  }
  if (*(int *)(&DAT_0804a050 + iVar1 * 4) != 0) {
    (*(code *)**(undefined4 **)(&DAT_0804a050 + iVar1 * 4))
              (*(undefined4 *)(&DAT_0804a050 + iVar1 * 4));
  }
  if (local_10 != *(int *)(in_GS_OFFSET + 0x14)) {
    __stack_chk_fail();
  }
  return;
}


┌─────────┐
| Exploit |
└─────────┘
Canary, NX, ASLR

