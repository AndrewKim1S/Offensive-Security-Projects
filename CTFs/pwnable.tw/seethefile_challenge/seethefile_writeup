
┌─────────────┬───────────────────────────[ seethefile Writeup ]──────────────────────────────────────────
| Description |
└─────────────┘
Leverage pwninit to get the correct ld (rename libc so file to libc.so.6) 
wget -O pwninit https://github.com/io12/pwninit/releases/download/3.3.0/pwninit
chmod +x pwninit
./pwninit --bin seethefile --libc libc.so.6

main() {
  int iVar1;
  int in_GS_OFFSET;
  EVP_PKEY_CTX *in_stack_ffffffc0;
  char input [32];
  undefined4 local_14;
  undefined1 *puStack_c;
  puStack_c = &stack0x00000004;
  local_14 = *(undefined4 *)(in_GS_OFFSET + 0x14);
  init(in_stack_ffffffc0);
  welcome();
  do {
    menu();
    __isoc99_scanf(&DAT_08048dd9, input);                    // get input 
    iVar1 = atoi(input);
    switch(iVar1) {
    default:
      puts("Invaild choice");
      exit(0);                                               // Invalid choice -> exit()
    case 1:
      openfile();                                            // 1 -> openfile()
      break;
    case 2:
      readfile();                                            // 2 -> readfile()
      break;
    case 3:
      writefile();                                           // 3 -> writefile()
      break;
    case 4:
      closefile();                                           // 4 -> closefile()
      break;
    case 5:
      printf("Leave your name :");                           // 5 -> get name, print name, close file, exit()
      __isoc99_scanf(&DAT_08048dd9,name);                    // Overflow
      printf("Thank you %s ,see you next time\n",name);
      if (fp != (FILE *)0x0) {
        fclose(fp);
      }
      exit(0);
    }
  } while( true );
}

int openfile() {
  int iVar1;
  char *pcVar2;
  if (fp == (FILE *)0x0) {                                   // check that no files are open 
    memset(magicbuf,0,400);                                  // memset magicbuf
    printf("What do you want to see :");
    __isoc99_scanf(&DAT_08048c03,filename);                  // get filename
    pcVar2 = strstr(filename,"flag");                        // don't read anything that has flag in the name
    if (pcVar2 != (char *)0x0) {
      puts("Danger !");
      exit(0);
    }
    fp = fopen(filename,"r");                                // open filename
    if (fp == (FILE *)0x0) {
      iVar1 = puts("Open failed");
    }
    else {
      iVar1 = puts("Open Successful");
    }
  }
  else {                                                     // make sure file (fp) is empty
    puts("You need to close the file first");
    iVar1 = 0;
  }
  return iVar1;
}

readfile() {
  size_t sVar1;
  memset(magicbuf,0,400);                                    // memset magicbuf
  if (fp == (FILE *)0x0) {                                   // check that file has been opened
    puts("You need to open a file first");
  }
  else {
    sVar1 = fread(magicbuf,399,1,fp);                        // read 399 bytes from fp to magicbuf
    if (sVar1 != 0) {
      puts("Read Successful");
    }
  }
  return;
}

writefile() {
  char *pcVar1;
  pcVar1 = strstr(filename,"flag");                          // check if filename includes flag 
  if (pcVar1 == (char *)0x0) {                               // do not continue if the file has flag in the name
    pcVar1 = strstr(magicbuf,"FLAG");                        // check if magicbuf includes flag
    if (pcVar1 == (char *)0x0) {                             // do not continue if the magicbuf has flag 
      pcVar1 = strchr(magicbuf,0x7d);                        // check if magicbuf includes 0x7d
      if (pcVar1 == (char *)0x0) {                           // do not continue if the magicbuf has 0x7d
        puts(magicbuf);                                      // print magicbuf
        return;
      }
    }
  }
  puts("you can\'t see it");
  exit(1);
}

closefile() {
  if (fp == (FILE *)0x0) {                                   // check if a file is open
    puts("Nothing need to close");
  }
  else {                                                     // close file
    fclose(fp);
  }
  fp = (FILE *)0x0;
  return;
}

┌─────────┐
| Context |
└─────────┘
The /proc/self directory is special dynamic shortcut. Points to process ID and allows program to inspect its own status. Includes
memory, absolute paths, file descriptors etc

In the GLIBC 2.23 source code (https://elixir.bootlin.com/glibc.glibc-2.23/source)
```
stdio.h:48
  typedef struct _IO_FILE FILE;
stdio.h:127
  # define fclose(fp) _IO_new_fclose(fp)

libio.h:241
  struct _IO_FILE {
    int flags;
    char* _IO_read_ptr;
    char* _IO_read_end;
    char* _IO_read_base;
    char* _IO_write_base;
    char* _IO_write_ptr;
    char* _IO_write_end;
    char* _IO_buf_base;
    char* _IO_buf_end;
    char* _IO_save_base;
    char* _IO_backup_base;
    char* _IO_save_end;

    struct _IO_marker *_markers;
    struct _IO_FILE *_chain;
    int _fileno;
    ...
    _IO_lock_t *_lock;
  }

libioP.h:342
  struct _IO_FILE_PLUS {
    _IO_FILE file;
    const struct _IO_jump_t *vtable
  };
libioP.h:307
  struct _IO_jump_t {
    JUMP_FIELD(size_t, __dummy);
    JUMP_FIELD(size_t, __dummy2);
    JUMP_FIELD(_IO_finish_t, __finish);                      // fclose()
    JUMP_FIELD(_IO_overflow_t, __overflow);
    JUMP_FIELD(_IO_underflow_t, __underflow);
    JUMP_FIELD(_IO_uflow_t, __uflow);
    JUMP_FIELD(_IO_pbackfail_t, __pbackfail);
    JUMP_FIELD(_IO_xsputn_t, __xsputn);
    JUMP_FIELD(_IO_xsgetn_t, __xsgetn);
    JUMP_FIELD(_IO_seekoff_t, __seekoff);
    JUMP_FIELD(_IO_seekpos_t, __seekpos);
    JUMP_FIELD(_IO_setbuf_t, __setbuf);
    JUMP_FIELD(_IO_sync_t, __sync);
    JUMP_FIELD(_IO_doallocate_t, __doallocate);
    JUMP_FIELD(_IO_read_t, __read);
    JUMP_FIELD(_IO_write_t, __write);
    JUMP_FIELD(_IO_seek_t, __seek);
    JUMP_FIELD(_IO_close_t, __close);
    JUMP_FIELD(_IO_stat_t, __stat);
    JUMP_FIELD(_IO_showmanyc_t, __showmanyc);
    JUMP_FIELD(_IO_imbue_t, __imbue); 
  }

iofclose.c:37
  int _IO_new_fclose(_IO_FILE *fp) {
    ...
    _IO_FINISH(fp);                                          // Jump to __finish vtable
    ...
  }
```


┌─────────┐
| Exploit |
└─────────┘
ASLR, NX, Partial RELRO, 32 bit, GLIBC 2.23

Leak
Because any file can be read and output to the terminal read /proc/self/maps - memory mapping of virtual memory for the process
Repeating 2 (read) and 3 (write) we can move forward through the file and read the entire memory map for the process

Overflow
When inputting name - there is a buffer overflow at the last get name scanf (.data or .bss sections)
  name is at 0x0804b260 (overtarget) - 32 bytes
  fp   is at 0x0804b280 (overflow target) - 4 bytes to (fp + 0x4 [0x0804b284])
  Forge File


Using the Leaked vmmap arrange the overflow to the following
  (A * 32) + (fp + 4) + (forged file)
  where fp is 0x20 + .data or .bss section
